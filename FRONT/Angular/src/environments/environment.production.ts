export const environment = {
  production: true,
  // Same origin by default (e.g. reverse-proxied behind the API in production).
  // Override at build time by swapping this file if the API lives elsewhere.
  apiBaseUrl: '/api',
};
